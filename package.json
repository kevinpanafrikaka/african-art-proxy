{
  "name": "african-art-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy that archives Smithsonian Open Access African art objects into D1 and serves them to a mobile app.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.19.1"
  }
}
